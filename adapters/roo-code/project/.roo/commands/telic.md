---
description: Run the current request through Telic's evidence-backed workflow
argument-hint: <coding request>
---

Use the `telic` skill and the connected `telic` MCP server for the current user
request. Preserve the request exactly, select the narrowest authorized mode, and
follow controller-directed typed handoffs through evidence-backed reporting.
Treat MCP as a deterministic ledger and validator, not as another reasoning
model. Do not claim that Telic intercepts Roo Code's native tools.
